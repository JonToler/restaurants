<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <meta charset="utf-8">
    <title>Restaurants</title>
  </head>
  <body>
    <div class="container">
      <div class="row">
        <div class="col-xs-6">
          <h1>Welcome to the Restaurant Repository!</h1>

          <p>Search for a Cuisine type: (eg: Hamburgers, Italian)</p>

          <form action="/cuisine_search" method="post">
              <label for="cuisine">Cuisine Type:</label>
              <input id="cuisine" name="cuisine" type="text">

              <button type="submit">Search Cuisine</button>
          </form>
          {% if not_found is not empty %}
          <h3>{{not_found}}</h3>
          <ul>
            {% for term in not_found_terms %}
            <li>{{term}}</li>
            {% endfor %}
          </ul>

          <p><a href="/add_cuisine">Add cuisine now?</a></p>
          {% endif %}
          {% if restaurants is not empty %}
          <h2>Restaurants of these types of cuisine:</h2>
          <ul>
            {% for restaurant in restaurants %}
            <li>{{restaurant.name}}({{restaurant.rating}}) - {{restaurant.description}} - {{restaurant.location}}</li>
            {% endfor %}
          </ul>
          {% endif %}
        </div>
        <div class="col-xs-6">
        </div>
      </div>
    </div>
  </body>
</html>
